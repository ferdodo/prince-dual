{
  "scripts": {
    "build": "make && tsc"
  },
  "devDependencies": {
    "svelte-compiler-cli": "../../lib/svelte-compiler-cli",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "svelte": "^4.1.1",
    "offline": "../../lib/offline",
    "config": "../../lib/config"
  }
}
