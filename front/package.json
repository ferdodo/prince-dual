{
  "scripts": {
    "build": "tsc && esbuild --bundle src/index.ts --outfile=public/bundle.js"
  },
  "devDependencies": {
    "core": "file:../core",
    "esbuild": "^0.17.19",
    "rxjs": "^7.8.1",
    "typescript": "^5.0.4"
  },
  "dependencies": {
    "connection-types": "^1.0.0"
  }
}
